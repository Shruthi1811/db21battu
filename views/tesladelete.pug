xtends layout 
 
block content 
  h1= title 
 
  if (toShow == null) 
    h2 No instance to delete 
  else 
    - let teslaid = toShow._id 
    - let Model = toShow.Model
    - let Year = toShow.Year 
    - let Price = toShow.Price 
    - let actionid = "/resource/tesla/" + modelid 
    script let actionid = "#{actionid}" 
    div 
      div.row 
        div(class='teslaAttr mr-3') Model ID is: 
        div #{modelid} 
      div.row 
        div(class='teslaAttr mr-3') Model Type is: 
        div #{Model} 
      div.row 
        div(class='teslaAttr mr-3') Model Year is: 
        div #{Year} 
      div.row 
        div(class='teslaAttr mr-3') Model Price is: 
        div #{Price} 
 
    button(class="btn btn-danger m-2" onclick="deleteOnid()") Delete 
    button(class="btn btn-secondary m-2" onclick="cancel()") Cancel 
 
  script. 
    function cancel() { 
      // Go back to the home page for the costume collection 
      window.location.href='/tesla' 
    } 
  script. 
    function deleteOnid() { 
      console.log("starting delete") 
      // delete first 
      fetch(actionid, { 
        method: 'DELETE'     
      }).then((resp) => { 
        return resp.json(); // or resp.text() or whatever the server sends 
      }).then((body) => { 
        alert("Delete succeded") 
        window.location.href='/tesla' 
        // TODO handle body 
      }).catch((error) => { 
        // TODO handle error 
        alert("Delete failed " + error ) 
      }); 
    } 